<div class="bg-light p-5 rounded-lg m-3">
  <h1 class="display-4"> <%= @contato.first_name %>  <%= @contato.last_name %></h1>

  <p>
    <strong>Email:</strong>
    <%= @contato.email %>
  </p>

  <p>
    <strong>Phone:</strong>
    <%= @contato.phone %>
  </p>

  <%= link_to 'Edit', edit_contato_path(@contato), class:"btn btn-secondary"  %> |
  <%= link_to 'Back', contatos_path, class:"btn btn-dark"  %>

</div>
